import { DateOfThePastDirective } from './date-of-the-past.directive';

describe('DateOfThePastDirective', () => {
  it('should create an instance', () => {
    const directive = new DateOfThePastDirective();
    expect(directive).toBeTruthy();
  });
});
